// SISTEMAS OPERATIVOS AI2 RubenServando

// prevenir inclusion multiple del encabezado
#ifndef carreraAtletismo_h
#define carreraAtletismo_h

// Incluye las bibliotecas necesarias para el programa
#include <stdio.h>     // Para las funciones de entrada y salida estándar (printf y scanf)
#include <stdlib.h>    // Para funciones de uso general (exit)
#include <unistd.h>    // Para las funciones de Unix (fork y usleep)
#include <sys/types.h> // Para definiciones de tipos de datos
#include <sys/ipc.h>   // Para el uso de memoria compartida y colas de mensajes
#include <sys/shm.h>   // Para funciones de memoria compartida (shmget y shmat)
#include <sys/wait.h>  // Para funciones de espera (wait)
#include <semaphore.h> // Para funciones de semáforo (sem_open y sem_wait)

// finalizar la prevención de inclusion multiple
#endif // carreraAtletismo_h